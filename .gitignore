.venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
# Data se nekomitují; vzorky v tests/data/ ano (vyrobí je tests/data/make_sample.py).
# Lomítko vpředu je podstatné: `data/` sedí i na tests/data/, kde jsou vzorky testů.
/data/

# Terraform: stav obsahuje ARN i jména bucketů, .terraform je 500 MB providerů.
infra/.terraform/
infra/*.tfstate
infra/*.tfstate.*
infra/*.tfvars
infra/tfplan

# Stránka je build artefakt: vyrábí ji web/build.py ze stavu curated, ne z gitu.
web/dist/

# wrangler si sem odklada stav mezi nasazenimi.
.wrangler/
